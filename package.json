{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "banking-ledger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm --filter api start:dev\" \"pnpm --filter web dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "prisma:generate": "pnpm --filter api exec prisma generate",
    "prisma:migrate": "pnpm --filter api exec prisma migrate dev",
    "prisma:studio": "pnpm --filter api exec prisma studio",
    "prisma:push": "pnpm --filter api exec prisma push",
    "prisma:pull": "pnpm --filter api exec prisma db pull",
    "prisma:generate-resources": "pnpm --filter api prisma:generate-resources"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "@typescript-eslint/parser": "^5.0.1",
    "eslint": "^8.7.0",
    "prettier": "^2.5.1",
    "turbo": "^2.8.1",
    "typescript": "^4.4.4"
  }
}
